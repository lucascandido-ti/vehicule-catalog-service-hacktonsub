/**
 * Base class for representing a **Domain Event**:
 *
 * > Use domain events to explicitly implement side effects of changes within your domain. In other
 * > words, and using DDD terminology, use domain events to explicitly implement side effects across
 * > multiple aggregates.
 */
export abstract class DomainEvent {}
